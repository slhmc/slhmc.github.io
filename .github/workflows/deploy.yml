name: Bootstrap and deploy website

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: '24'
      - run: npm install
      - run: npm run images
      - run: npm test
      - run: npm run build
      - name: Persist generated files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json src/image-manifest.ts \
            public/assets/preview-optimized/EN/S-Privacy.webp \
            public/assets/preview-optimized/EN/S-Privacy-mobile.webp \
            public/assets/preview-optimized/EN/S-Storage.webp \
            public/assets/preview-optimized/EN/S-Storage-mobile.webp \
            public/assets/preview-optimized/EN/S-Sync.webp \
            public/assets/preview-optimized/EN/S-Sync-mobile.webp
          git diff --cached --quiet || git commit -m "Add generated website assets"
          git push origin HEAD:main
      - uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6
      - uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5
        with:
          path: dist
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@368f82528645a54fb793d4d04e342629a3f51346 # v5
